# Process Manager Frontend

## Docker

You can build this individual image with: `docker build . -t process-manager-frontend`

Then run it with: `docker run -d -p 8080:8080 process-manager-frontend`

## NPM

- Run the app with `npm start`